<!-- component -->
<a href="{{ path('app_show', {'id': id}) }}">
	<div class="container mx-auto shadow-lg rounded-lg max-w-md hover:shadow-2xl transition duration-300">
		<img src="{{ image }}" alt="" class="rounded-t-lg w-full">
		<div class="p-6">
			<h1 class="md:text-1xl text-xl hover:text-indigo-600 transition duration-200  font-bold text-gray-900 ">{{ title }}</h1>
			<p class="text-gray-700 my-2 hover-text-900 ">{{content}}</p>
		</div> 
	</div>
</a>
